import React from 'react'
import { View, StyleSheet } from 'react-native'

export default Status = () => {
    return (
        <View style={styles.bar}>

        </View>
    );
}

const styles = StyleSheet.create({
    bar: {
        flex: 1,
        maxHeight: 24.5,
        backgroundColor: '#25a861'
    }
})